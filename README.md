# codex-playground
Playground for codex

## GitHub Pages
This repo is ready for GitHub Pages using the `/docs` folder. Configure GitHub Pages to serve from `main` (or the current default branch) and `/docs` to publish the meal planner online. The published site will use `docs/index.html`.
